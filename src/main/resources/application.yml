spring:
  application:
    name: LMX

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: "0521"
    url: jdbc:mysql://localhost:3306/jpa?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true

  jpa:
    show-sql: true
    #generate-ddl: true

  mail:
    host: localhost
    port: 1025

backend:
  menus:
    - {name: users, url: /admin/users}
    - {name: posts, url: /admin/posts}